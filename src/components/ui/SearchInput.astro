```astro
---
interface Props {
  placeholder?: string;
  name?: string;
  value?: string;
  class?: string;
}

const { 
  placeholder = 'Search...', 
  name = 'search',
  value,
  class: className,
} = Astro.props;
---

<div class:list={['relative', className]}>
  <svg 
    class="absolute left-3 top-1/2 -translate-y-1/2 w-5 h-5 text-secondary-gray pointer-events-none"
    fill="none" 
    stroke="currentColor" 
    viewBox="0 0 24 24"
  >
    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"/>
  </svg>
  
  <input
    type="search"
    name={name}
    placeholder={placeholder}
    value={value}
    class="w-full pl-10 pr-4 py-3 rounded-full border border-light-gray bg-white text-navy placeholder:text-secondary-gray transition-all duration-fast focus:outline-none focus:border-gold focus:ring-2 focus:ring-gold/50"
  />
</div>
```

---