---
import Layout from '../layouts/Layout.astro';
import Button from '../components/ui/Button.astro';
import Card from '../components/ui/Card.astro';
import Badge from '../components/ui/Badge.astro';
import Section from '../components/ui/Section.astro';
import Hero from '../components/ui/Hero.astro';
import TearsheetMockup from '../components/TearsheetMockup.astro';
import { Image } from 'astro:assets';
import appScreenshot from '../assets/images/homepage/app-screenshot.png';
import { getCollection } from 'astro:content';

// Fetch featured articles for homepage
const allReports = await getCollection('reports');
const featuredReports = allReports
  .filter(report => !report.id.startsWith('_')) // Exclude template files
  .filter(report => report.data.featured === true)
  .sort((a, b) => new Date(b.data.date).valueOf() - new Date(a.data.date).valueOf())
  .slice(0, 3);
---

<Layout
  title="Fund Investigator - Comprehensive Mutual Fund Analysis"
  description="Detailed performance metrics and risk analysis for AMFI-registered funds. Data-driven insights to support your investment decisions."
>
  <!-- Hero Section -->
  <Hero size="md">
    <!-- Oversized heading with breathing room -->
    <div class="mb-16">
      <h1 class="text-4xl sm:text-5xl md:text-6xl lg:text-display-xl font-bold text-navy mb-8 leading-[1.1] tracking-tight break-words">
        Mutual Fund Performance,
        <span class="text-navy/60">Made Clear</span>
      </h1>

      <!-- Reduced subtitle - single line, essential info only -->
      <p class="text-body-lg text-secondary-gray max-w-2xl mx-auto font-light leading-relaxed">
        Fund Investigator's <span class="text-gold font-semibold">Deepdive</span> app and <span class="text-gold font-semibold">investigations</span> provide comprehensive metrics and risk analysis for Indian Mutual Funds—all in one place.
      </p>
    </div>

    <!-- CTAs -->
    <div class="flex flex-col sm:flex-row justify-center items-center gap-4 mb-20 px-4">
      <Button href="https://deepdive.fundinvestigator.com" size="lg" icon="arrow-right" target="_blank" rel="noopener noreferrer" class="w-full sm:w-auto">
        <span class="hidden sm:inline">Launch Deepdive</span>
        <span class="sm:hidden">Launch App</span>
      </Button>
      <Button href="/reports" variant="secondary" size="lg" icon="arrow-right" class="w-full sm:w-auto">
        <span class="hidden sm:inline">Browse Investigations</span>
        <span class="sm:hidden">View Reports</span>
      </Button>
    </div>

    <!-- Solution pillars -->
    <div class="grid md:grid-cols-3 gap-8 max-w-4xl mx-auto">
      <div class="text-center space-y-3">
        <div class="flex justify-center">
          <svg class="w-10 h-10 text-gold" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"/>
          </svg>
        </div>
        <h3 class="text-heading-sm font-semibold text-navy">All KPIs in One Place</h3>
        <p class="text-body-sm text-secondary-gray leading-relaxed">Comprehensive metrics without jumping between sources</p>
      </div>
      <div class="text-center space-y-3">
        <div class="flex justify-center">
          <svg class="w-10 h-10 text-gold" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M4 5a1 1 0 011-1h4a1 1 0 011 1v7a1 1 0 01-1 1H5a1 1 0 01-1-1V5zM14 5a1 1 0 011-1h4a1 1 0 011 1v7a1 1 0 01-1 1h-4a1 1 0 01-1-1V5zM4 16a1 1 0 011-1h4a1 1 0 011 1v3a1 1 0 01-1 1H5a1 1 0 01-1-1v-3zM14 16a1 1 0 011-1h4a1 1 0 011 1v3a1 1 0 01-1 1h-4a1 1 0 01-1-1v-3z"/>
          </svg>
        </div>
        <h3 class="text-heading-sm font-semibold text-navy break-words">Multi-Dimensional & Multi-Period Insights</h3>
        <p class="text-body-sm text-secondary-gray leading-relaxed">Performance across timeframes and angles</p>
      </div>
      <div class="text-center space-y-3">
        <div class="flex justify-center">
          <svg class="w-10 h-10 text-gold" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
          </svg>
        </div>
        <h3 class="text-heading-sm font-semibold text-navy">No Sales, No Commissions</h3>
        <p class="text-body-sm text-secondary-gray leading-relaxed">Analysis only, no recommendations</p>
      </div>
    </div>
  </Hero>

  <!-- Deepdive Showcase Section -->
  <Section background="white" spacing="md" maxWidth="full">
    <div class="max-w-7xl mx-auto px-4 sm:px-6">
      <div class="bg-cream/50 rounded-2xl sm:rounded-3xl p-6 sm:p-12 lg:p-16">
        <div class="grid lg:grid-cols-2 gap-12 items-center">
          <!-- Left: Description -->
          <div class="space-y-6">
            <div>
              <h2 class="text-display-md font-bold text-navy">Deepdive</h2>
              <p class="text-body-sm text-secondary-gray mt-2">Comprehensive fund analysis app</p>
            </div>
            <p class="text-body-md text-secondary-gray leading-relaxed">
              Search any AMFI-registered fund and access detailed performance metrics. Risk-adjusted returns, drawdown analysis, rolling returns, and benchmark correlation in a single view.
            </p>
            <div>
              <Button href="https://deepdive.fundinvestigator.com" variant="secondary" size="md" target="_blank" rel="noopener noreferrer">Launch Deepdive</Button>
            </div>
          </div>

          <!-- Right: App Screenshot -->
          <div class="relative">
            <div class="aspect-[4/3] bg-white rounded-2xl shadow-xl overflow-hidden border border-gray-200">
              <Image
                src={appScreenshot}
                alt="Deepdive app showing mutual fund performance metrics and charts"
                class="w-full h-full object-cover"
                loading="eager"
                widths={[640, 1024, 1400]}
                sizes="(max-width: 768px) 100vw, 50vw"
              />
            </div>
          </div>
        </div>
      </div>
    </div>
  </Section>

  <!-- Investigations Section -->
  <Section background="white" spacing="md" maxWidth="full">
    <div class="max-w-7xl mx-auto px-4 sm:px-6">
      <div class="bg-gradient-to-br from-blue-50 to-teal-50 rounded-2xl sm:rounded-3xl p-4 sm:p-8 lg:p-16">
        <div class="grid lg:grid-cols-[1fr,1.2fr] gap-8 lg:gap-12 items-start">
          <!-- Left: Description -->
          <div class="space-y-6 lg:sticky lg:top-32">
            <h2 class="text-3xl sm:text-4xl lg:text-5xl font-bold text-navy break-words">Featured Investigations</h2>
            <p class="text-body-md text-secondary-gray leading-relaxed">
              In-depth analysis of fund performance, portfolio shifts, and risk patterns. New investigations published monthly.
            </p>
            <div>
              <Button href="/reports" variant="secondary" size="md">See all investigations</Button>
            </div>
          </div>

          <!-- Right: Featured investigation cards -->
          <div class="space-y-4">
            {featuredReports.length > 0 ? (
              featuredReports.map((report) => (
                <a href={`/reports/${report.slug}`} class="block group">
                  <div class="bg-white rounded-xl sm:rounded-2xl p-4 sm:p-6 shadow-sm hover:shadow-md transition-all duration-300 border border-gray-100">
                    <div class="flex items-start justify-between gap-3 sm:gap-4">
                      <div class="flex-1 min-w-0">
                        <Badge class="mb-2">Featured Investigation</Badge>
                        <h3 class="text-base sm:text-lg font-bold text-navy mb-2 leading-tight group-hover:text-gold transition-colors break-words">
                          {report.data.title}
                        </h3>
                        <p class="text-sm sm:text-base text-secondary-gray leading-relaxed line-clamp-2">
                          {report.data.description}
                        </p>
                      </div>
                      <svg class="w-4 h-4 sm:w-5 sm:h-5 text-navy group-hover:text-gold transition-colors flex-shrink-0 mt-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
                      </svg>
                    </div>
                  </div>
                </a>
              ))
            ) : (
              <div class="bg-white rounded-2xl p-6 sm:p-8 text-center border border-gray-100">
                <p class="text-body-md text-secondary-gray">No featured investigations yet. Check back soon.</p>
              </div>
            )}
          </div>
        </div>
      </div>
    </div>
  </Section>

  <!-- About & Vision Section -->
  <Section id="about" background="white" spacing="md" maxWidth="full" class="scroll-mt-20">
    <div class="max-w-7xl mx-auto px-4 sm:px-6">
      <div class="bg-gradient-to-br from-navy/5 to-gold/5 rounded-2xl sm:rounded-3xl p-6 sm:p-12 lg:p-16">
        <div class="max-w-4xl mx-auto">
          <!-- Section Header -->
          <div class="text-center mb-12">
            <div class="flex justify-center mb-6">
              <div class="h-1 w-16 bg-gold rounded-full"></div>
            </div>
            <h2 class="text-display-md font-bold text-navy mb-6">About This Project</h2>
            <p class="text-body-lg text-secondary-gray leading-relaxed">
              Institutional-grade fund analysis available to all investors
            </p>
          </div>

          <!-- Key Points Grid -->
          <div class="grid md:grid-cols-3 gap-8 mb-16">
            <div class="text-center space-y-3">
              <div class="flex justify-center">
                <div class="w-14 h-14 bg-navy/10 rounded-2xl flex items-center justify-center">
                  <svg class="w-7 h-7 text-navy" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z"/>
                  </svg>
                </div>
              </div>
              <h3 class="text-heading-sm font-bold text-navy">Transparent Data</h3>
              <p class="text-body-sm text-secondary-gray leading-relaxed">
                AMFI data updated daily. Standard financial measures with clear calculations—no black boxes.
              </p>
            </div>

            <div class="text-center space-y-3">
              <div class="flex justify-center">
                <div class="w-14 h-14 bg-navy/10 rounded-2xl flex items-center justify-center">
                  <svg class="w-7 h-7 text-navy" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"/>
                  </svg>
                </div>
              </div>
              <h3 class="text-heading-sm font-bold text-navy">Beyond Returns</h3>
              <p class="text-body-sm text-secondary-gray leading-relaxed">
                Risk-adjusted performance, drawdown patterns, and consistency across market cycles.
              </p>
            </div>

            <div class="text-center space-y-3">
              <div class="flex justify-center">
                <div class="w-14 h-14 bg-navy/10 rounded-2xl flex items-center justify-center">
                  <svg class="w-7 h-7 text-navy" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253"/>
                  </svg>
                </div>
              </div>
              <h3 class="text-heading-sm font-bold text-navy">Independent Analysis</h3>
              <p class="text-body-sm text-secondary-gray leading-relaxed">
                No commissions, no fund partnerships. Data-driven insights presented without recommendations.
              </p>
            </div>
          </div>

          <!-- Subtle link to About page -->
          <div class="text-center mt-12 pt-8 border-t border-gray-200">
            <a href="/about" class="text-navy hover:text-gold transition-colors text-body-sm font-medium inline-flex items-center gap-2">
              Why this project exists
              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
              </svg>
            </a>
          </div>

        </div>
      </div>
    </div>
  </Section>
</Layout>

<style>
  .line-clamp-2 {
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
</style>
